# MongoDB Atlas Connection
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/autoparts?retryWrites=true&w=majority

# Get your connection string from MongoDB Atlas → Database → Connect → Drivers
# Make sure to:
# 1. Replace 'username' with your database user
# 2. Replace 'password' with your database password  
# 3. Replace 'cluster' with your cluster name
